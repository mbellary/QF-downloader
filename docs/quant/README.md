# Quant Specification Package (Q0)

This folder contains the Quant-owned, normative contracts.

- Machine-readable files (`.yaml` written in JSON-compatible form, plus `.json`) are authoritative.
- Markdown files are explanatory only.

Global constitutional constraints: `docs/quant/global_system_constraints.yaml`.
Schema inventory: `docs/quant/schema_registry.yaml`.
