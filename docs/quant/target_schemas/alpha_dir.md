# Target Schema — alpha_dir_labels

Machine-readable authority: `docs/quant/target_schemas/alpha_dir.yaml`.

This schema defines the required fields and causality constraints for `targets/alpha_dir_labels.csv`.
