# Target Schema — macro_event_returns

Machine-readable authority: `docs/quant/target_schemas/macro_event.yaml`.

This schema defines required fields and allowed event classes for `targets/macro_event_returns.csv`.
