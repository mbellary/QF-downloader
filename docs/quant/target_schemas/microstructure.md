# Target Schema — microstructure_targets

Machine-readable authority: `docs/quant/target_schemas/microstructure.yaml`.

This schema defines the required fields and causality constraints for `targets/microstructure_targets.parquet`.
