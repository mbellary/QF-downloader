# Target Schema — path_outcomes

Machine-readable authority: `docs/quant/target_schemas/path_outcomes.yaml`.

This schema defines required fields and allowed outcome labels for `targets/path_outcomes.parquet`.
